import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'dev.umfahrer.recipeapp',
  appName: 'Cheeers!',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
